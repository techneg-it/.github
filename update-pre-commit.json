{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"description": "Enable `pre-commit` manager and group non-major updates",
	"extends": [
		"github>techneg-it/.github:group-by-manager(pre-commit,hooks)",
		":enablePreCommit"
	]
}
